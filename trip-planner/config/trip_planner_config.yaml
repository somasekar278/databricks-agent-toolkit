# Trip Planner Agent Configuration
#
# Generated from SOTA Agent Framework template.
# Customize this configuration for your deployment.

agents:
  trip_planner_agent:
    class: "trip_planner.TripPlannerAgent"
    enabled: true
    execution_mode: "ray_task"  # Options: in_process, process_pool, ray_task
    timeout: 30  # seconds
    retry_policy: "exponential"
    max_retries: 3
    
    # Custom configuration for your agent
    # Add any agent-specific parameters here
    # Example:
    # model_name: "your-model"
    # temperature: 0.7
