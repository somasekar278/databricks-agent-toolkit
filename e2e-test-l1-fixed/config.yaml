# e2e-test-l1-fixed - Chatbot Configuration
# Generated by: Databricks Agent Toolkit v0.1.0

agent_name: e2e-test-l1-fixed
type: chatbot

# Model configuration
model:
  endpoint: databricks-claude-sonnet-4-5  # Modern foundation model
  temperature: 0.7
  max_tokens: 500
  
  # Response mode
  streaming: false  # Set to true for token-by-token streaming

# MLflow tracking
mlflow:
  experiment: /Shared/e2e-test-l1-fixed
  auto_trace: true
  show_url: false  # Show MLflow URL after conversations (for developers)
  
  # Assessment collection (human feedback)
  assessments:
    enabled: true  # Set to false to disable
    frequency: 5  # Ask for feedback every N conversations (0 = never)
